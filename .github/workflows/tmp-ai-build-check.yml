name: Temporary AI build check

on:
  push:
    branches:
      - feature/ai-assessment-engine

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm install --ignore-scripts
      - run: npm run typecheck
      - run: npm run build
