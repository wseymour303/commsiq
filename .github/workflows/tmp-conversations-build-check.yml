name: Temporary Conversations build check

on:
  push:
    branches:
      - feature/conversations-workspace

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - run: npm install --ignore-scripts
      - run: npm run typecheck
      - run: npm run build
